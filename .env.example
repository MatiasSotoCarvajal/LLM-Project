# Override the default ./models directory path.
# Leave empty to use the default (project_root/models).
MODELS_DIR = "example_route"

# --- GPU configuration (for NVIDIA CUDA / Vast.ai) ---
# N_GPU_LAYERS = 999
# FLASH_ATTN = 1
# N_BATCH = 2048
# N_UBATCH = 512
